unit uControllerCarros;

interface
  uses ucontroller, uDM, DB;
  type Controllerlaboratorios = class(controller)
  private

  protected

  public
    constructor CrieObj;
    destructor  Destrua_se;
    function Salvar ( pObj :  TObject ):            string;    override;
    function Carregar ( pObj : TObject ):           string;    override;
    function Excluir ( pObj : TObject ):            string;    override;
    function Pesquisar ( pChave : string):          string;    override;
    procedure setDM (pDM : TDM);
    function getds : Tdatasource;
  end;

implementation

{ Controllerlaboratorios }

function Controllerlaboratorios.Carregar(pObj: TObject): string;
begin

end;

constructor Controllerlaboratorios.CrieObj;
begin

end;

destructor Controllerlaboratorios.Destrua_se;
begin

end;

function Controllerlaboratorios.Excluir(pObj: TObject): string;
begin

end;

function Controllerlaboratorios.getds: Tdatasource;
begin

end;

function Controllerlaboratorios.Pesquisar(pChave: string): string;
begin

end;

function Controllerlaboratorios.Salvar(pObj: TObject): string;
begin

end;

procedure Controllerlaboratorios.setDM(pDM: TDM);
begin

end;

end.
